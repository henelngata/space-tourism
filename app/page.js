import {Navigation, DisplayButton, SpaceText, MobileMenuNav } from "./ui/components";

export default function Page(params) {
  // nav
  //text
  //button
  return (

     
      <main>
        <section>
          <SpaceText />
          <DisplayButton/>
        </section>
      </main>
  );
}
